#include <cstdlib>
#include <iostream>
using namespace std;

long long f(int n){
    if (n < 2)
    return 1;
    return n*f(n-1);
}
int main(){
    int n;
    cout << "Podaj liczbe: "; cin >> n;
    cout << "Silnia z liczby "<<n<<" " << "to: " << f(n) << endl;
    return 0;
}
